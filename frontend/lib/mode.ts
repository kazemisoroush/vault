// MODE_STORAGE_KEY is where the chosen mode is persisted, shared by the pre-paint script and the toggle.
export const MODE_STORAGE_KEY = "vault-mode";

// Mode names the two faces of the app; the backend has no mode.
export type Mode = "personal" | "legal";
