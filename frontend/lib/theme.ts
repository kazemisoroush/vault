// THEME_STORAGE_KEY is where the chosen theme is persisted. It is read by the pre-paint
// script in the layout and written by the ThemeToggle, so both must use this one constant.
export const THEME_STORAGE_KEY = "vault-theme";
