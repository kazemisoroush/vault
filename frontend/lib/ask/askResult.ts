import type { components } from "../api/schema";

// AskResult is one matched file plus its presigned download URL, as defined by openapi.yaml.
export type AskResult = components["schemas"]["AskResult"];
